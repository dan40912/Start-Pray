import "@/styles/v3-wireframe.css";

export const metadata = {
  title: "V3 Wireframe 預覽",
  robots: { index: false, follow: false },
};

export default function V3WireframeLayout({ children }) {
  return children;
}
